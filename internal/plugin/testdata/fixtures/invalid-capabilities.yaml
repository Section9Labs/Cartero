schema_version: v1
name: report-bundle
version: 1.3.0
kind: exporter
mode: local-only
safe: true
capabilities:
  - results.export
  - preview.rendered
  - results.export
trust:
  level: reviewed
  review_required: false
description: Invalid capability set for fixture coverage.
